import React from 'react';
import {form, FormControl} from 'react-bootstrap';
 
const Search = () => (
    <form>
        <FormControl
            type="text"
        />
    </form>
);

export default Search;