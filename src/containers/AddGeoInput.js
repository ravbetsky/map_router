import React from 'react';
import {form, FormControl} from 'react-bootstrap';

const AddGeoInput = () => (
    <form>
        <FormControl
            type="text"
        />
    </form>
);

export default AddGeoInput;
